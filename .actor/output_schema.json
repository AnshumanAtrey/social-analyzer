{
    "actorOutputSchemaVersion": 1,
    "title": "Social Analyzer output",
    "description": "Profiles found for each username, one row each with confidence, category and country of the site, plus a run summary.",
    "type": "object",
    "properties": {
        "profiles": {
            "type": "string",
            "title": "Profiles found (dataset)",
            "description": "One row per matched profile: username, platform, site, link, confidence (high/medium/low), matchRate, category, categoryDetail, country, siteRank, pageTitle, pageText, extracted, metadata, checkedAt. Adult and dating sites are never checked. The last row (recordType = summary) holds the run totals and per-username notes.",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "summary": {
            "type": "string",
            "title": "Run summary (JSON)",
            "description": "Totals, filters applied, per-username results, notes about cleaned or skipped input, and the plain-language message shown as the run status.",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
        },
        "log": {
            "type": "string",
            "title": "Run log",
            "description": "What was checked, per username, with timings and any scanner warnings.",
            "template": "{{links.publicRunUrl}}/log"
        }
    }
}
