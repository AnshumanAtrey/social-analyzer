{
  "$comment": "Store listing fields that apify push never sends. Applied by scripts/store-metadata.mjs after every deploy. Title, description and categories live in actor.json.",
  "seoTitle": "Social Analyzer - Find Social Media by Username | Sherlock",
  "seoDescription": "Find someone's social media profiles from a username, across 900+ sites. Confidence-ranked, no login or API key. Paste a handle or link, we clean it. For recruiting, fraud checks, people search.",
  "exampleRunInput": {
    "username": "elonmusk"
  },
  "actorPermissionLevel": "LIMITED_PERMISSIONS",
  "isPublic": true,
  "defaultRunOptions$comment": "Full-depth default runs 8 scanner processes at once; on the platform they peaked at 2046 MB of a 2 GB run and two steps were OOM-killed (exit -9), losing 250 sites (build 1.0.8 test run, 2026-09-12: 175 s, 206 profiles vs 289 locally). 4 GB gives ~2 GB headroom and a full vCPU. Compute per full run at 4 GB is about 0.2 CU ($0.04) against ~200 charged rows ($0.80 developer share). Timeout 3600 matches the input's one-hour maximum.",
  "defaultRunOptions": {
    "build": "latest",
    "memoryMbytes": 4096,
    "timeoutSecs": 3600
  },
  "pricing": {
    "apply": true,
    "$comment": "v1.1.0 moves charging from apify-default-dataset-item ($0.005 per row incl. the summary) to a custom 'profile' event at the same $0.005, charged per step as rows are delivered; the summary row becomes free. Applied via API on 2026-09-12: the platform refused an immediate start (cannot-modify-actor-pricing-with-immediate-effect: a public Actor with users needs notice), so the record is scheduled to start 2026-09-26T22:14Z. Until then rows are still charged per dataset item by the platform and the code's 'profile' charges log a harmless unknown-event warning. The CI script will report the future record until it starts, then 'live prices match'. Event name must equal PROFILE_EVENT in src/main.py.",
    "pricingInfos": [
      {
        "pricingModel": "PAY_PER_EVENT",
        "apifyMarginPercentage": 0.2,
        "reasonForChange": "Same $0.005 per profile found, now charged step by step as rows are delivered instead of per dataset row; the summary row is free and cut-off runs pay only for what arrived.",
        "pricingPerEvent": {
          "actorChargeEvents": {
            "profile": {
              "eventTitle": "Profile found",
              "eventDescription": "One profile row: platform, URL, confidence, match rate, site category and country, page title, language and meta tags. Charged as each step of the scan delivers its rows. The summary row is free.",
              "eventPriceUsd": 0.005
            }
          }
        }
      }
    ]
  }
}
